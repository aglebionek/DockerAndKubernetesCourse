{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "i": "cd client && rm -rf node_modules && npm i && cd .. && cd server && rm -rf node_modules && npm i && cd .. && cd worker && rm -rf node_modules && npm i && cd .."
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
